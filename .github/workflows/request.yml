name: Keep Alive

on:
  schedule:
    - cron: '*/20 * * * *'

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl -s https://footystats-proxy.onrender.com/cache-status
          curl -s https://footystats-proxy.onrender.com/preds?tz=0
